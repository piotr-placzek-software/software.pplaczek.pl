- trimm spaces for command string from input
- improve error handling: define generic errors and handle them directly in commands services
- support for -h command switch
- support Tab autocompletion
- support CtrlA for moving cursor at the begin of input
- support CtrlK for clearing input value if cursor is at the begin of input
- support CtrlW for removing word at the left from cursor
- set cursor at the end of input value when user takes value from history
- add file about fpv-calc
- create command for quick-links: take link name from first argument and open corelated webpage at new tab
- add contact info as file and command (with parameters for displayng only selected info)
